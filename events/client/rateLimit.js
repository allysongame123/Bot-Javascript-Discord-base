//here the event starts
module.exports = (client, rateLimitData) => {
    console.log(JSON.stringify(rateLimitData).grey.italic.dim);
    console.log("Se estiver usando replit va ate o shell digite >kill 2<")
}
